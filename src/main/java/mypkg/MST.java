package mypkg;

import java.util.*;

public class MST {

    void foo() {
        String s = "a".replaceAll("a", "b").replaceAll("x", "y");
        List<String> list = Collections.<String>emptyList();
        String replaceAll = "";
        replaceAll.replaceAll("a","b");
        replaceAll. replaceAll("c","d");
        replaceAll .replaceAll("e","f");
    }
}
